# Schemas Package

## Start Here

Schema artifacts shared across app, service, prompt, and eval boundaries live here. Keep them explicit and versionable.

Useful command:

```bash
pnpm --filter @ancora/schemas typecheck
```
