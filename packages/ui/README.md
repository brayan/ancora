# UI Package

## Start Here

Shared React UI primitives live here when more than one app needs them. Keep product workflow behavior in app or service boundaries.

Use `PascalCase` filenames for React component primitives and keep exports explicit.

Useful command:

```bash
pnpm --filter @ancora/ui typecheck
```
