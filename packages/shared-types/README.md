# Shared Types

## Start Here

Shared TypeScript contracts live here when they are needed across package or app boundaries. Do not hide backend business logic in this package.

Useful command:

```bash
pnpm --filter @ancora/shared-types typecheck
```
