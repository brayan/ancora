# Config Package

## Start Here

Shared non-secret configuration constants live here. Runtime secrets and environment-specific values belong in environment variables and `.env.example` templates.

Useful command:

```bash
pnpm --filter @ancora/config typecheck
```
