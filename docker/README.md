# Docker

## Start Here

Local Docker Compose configuration lives here. The current foundation is PostgreSQL with pgvector only.

Useful commands:

```bash
docker compose -f docker/docker-compose.yml config
make docker-up
make docker-down
```
