# Config

## Start Here

Repository-level non-secret configuration artifacts belong here when they are not package code. Use `.env.example` files for environment shape and keep real secrets out of the repo.
